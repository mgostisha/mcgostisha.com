---
export interface Props {
    classes?: string;
    external?: boolean;
    to: string;
}

const { classes, external, to, ...rest } = Astro.props;
---

<a class={`link ${classes ?? ""}`} href={to} target={external ? "_blank" : undefined} {...rest}><slot /></a>

<style>
    .link {
        color: var(--site-interactive);
    }
</style>
